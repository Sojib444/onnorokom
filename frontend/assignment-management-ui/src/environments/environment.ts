/**
 * Development environment. The API runs locally on port 5105; the backend already
 * allows this origin through its CORS policy.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:5105',
};
